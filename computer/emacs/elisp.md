# Output

```
(print {object})
```

# Functions

Call a function (as a variable), passing parameters:
```
(funcall {fn} {p1} {p2} ...)
```

# Variables

let
```
(let ({decalarations}) ...)
```

Declare only:
```
(let ({var1, var2}) ...)
```

Assign:
```
(let (({var1} {expression1})) ...)
```
